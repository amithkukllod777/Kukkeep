<?xml version="1.0" encoding="utf-8"?>
<!-- KukKeep home-screen widget layout. CI copies this to res/layout/notes_widget.xml.
     Header (brand + "＋ new note") over up to 3 recent notes. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="14dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Kuk Keep"
            android:textColor="#2868F0"
            android:textStyle="bold"
            android:textSize="13sp" />
        <TextView
            android:id="@+id/widget_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="＋"
            android:textColor="#2868F0"
            android:textStyle="bold"
            android:textSize="22sp"
            android:paddingStart="10dp"
            android:paddingEnd="6dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="No notes yet — tap ＋ to add one"
        android:textColor="#667085"
        android:textSize="13sp"
        android:visibility="gone" />

    <TextView
        android:id="@+id/widget_note1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#101828"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/widget_note2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#101828"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/widget_note3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#101828"
        android:textSize="14sp" />
</LinearLayout>
